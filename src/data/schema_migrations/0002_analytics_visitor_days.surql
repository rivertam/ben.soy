-- Epoch 2 formerly installed the analytics visitor-day read model. The
-- analytics stack has been removed; this epoch is retained as a no-op so
-- existing site_schema_migrations ledgers stay contiguous.
